version: "3.0"

services:
  panel:
    build: .
    volumes:
      - ./:/usr/src/app
    restart: always
    ports:
      - 5000:5000
    environment:
      - MAINTENANCE_MODE=False
      - DEVELOPMENT_MODE=False
      - SECRET_KEY=RANDOM_KEY